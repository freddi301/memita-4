npm run format &&\
git diff --exit-code || { echo "❌ You have unstaged changes. Please stage all files before committing."; exit 1; } &&\
test -z "$(git ls-files --others --exclude-standard)" || { echo "❌ You have untracked files. Please stage or ignore them before committing."; exit 1; } &&\
npm run build:bare &&\
npm run typecheck &&\
npm run lint &&\
npm run knip || { echo "❌ Found unused exports"; exit 1; } &&\
npm run test &&\
npx expo export  &&\
npx expo prebuild --clean &&\
npm run lint-staged