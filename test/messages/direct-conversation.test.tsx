// --- AI proposals: 2026-05-05 ---

test.todo(
  "user sees their own name on messages they sent and the contact name on messages received",
);

test.todo(
  "user sees a draft indicator on a message that has not been sent yet",
);

test.todo("user sees an unread indicator on a message received from a contact");

test.todo(
  "user long-presses one of their own messages to select it for editing",
);

test.todo(
  "user edits a sent message, the updated text replaces the original in the conversation",
);

test.todo("user sees message timestamps displayed next to each message");

test.todo(
  "user can navigate back to the direct messages list from an open conversation",
);

test.todo(
  "user can tap the contact name in the conversation header to open the contact profile",
);
